# Test Environment Configuration
# This file is used during test execution to isolate test data from development/production

# Database Configuration (Test)
# Use a separate test database to avoid polluting development data
DATABASE_URL="postgresql://test:test@localhost:5432/test_db"

# Redis Configuration (Test)
# Mock Redis endpoints for testing
UPSTASH_REDIS_REST_URL="https://test.upstash.io"
UPSTASH_REDIS_REST_TOKEN="test-token-placeholder"

# API Configuration (Test)
NEXT_PUBLIC_API_BASE_URL="http://localhost:3000"

# N8N Webhook Configuration (Test)
N8N_WEBHOOK_URL="http://localhost:5678/webhook-test/lead-ingest"
N8N_API_KEY="test-n8n-api-key"

# Rate Limiting (Test)
RATE_LIMIT_MAX_REQUESTS="100"
RATE_LIMIT_WINDOW_MS="60000"

# Feature Flags (Test)
NEXT_PUBLIC_ENABLE_ANALYTICS="false"
NEXT_PUBLIC_ENABLE_DEBUG="true"
